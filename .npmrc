# @joezilla/8sim publishes to GitHub Packages. The token is injected via the
# NODE_AUTH_TOKEN env var (CI: secrets.GITHUB_TOKEN; local: `gh auth token`),
# so no secret is ever written to disk.
@joezilla:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
